on:
  push:
      branches:
        - main

jobs:
  revise_readme:
    runs-on: ubuntu-latest
    steps:          
        - name: Commit and push changes
          if: steps.changed-files.outputs.files_changed == 'true'
          run: |
             echo ${{github.actor}}
             echo ${{var.email}}
