on:
  push:
    branches:
      - main

jobs:
  if_merged:
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo The PR was merged
        echo ${{ vars.EMAIL }}
        echo ${{vars.NAME}}
